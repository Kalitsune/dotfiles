general {
    immediate_render = true
}

background {
    monitor =
    path = $HOME/.cache/wallpaper.img   # only png supported for now
    color = rgba(25, 20, 20, 1.0)

    # all these options are taken from hyprland, see https://wiki.hyprland.org/Configuring/Variables/#blur for explanations
    blur_passes = 1 # 0 disables blurring
    blur_size = 6
    noise = 0
    contrast = 0.8916
    brightness = 0.8172
    vibrancy = 0.1696
    vibrancy_darkness = 0.0
}

auth {
    fingerprint {
        enabled = true
        retry_delay	= 100
    }
}

input-field {
    monitor =
    size = 500, 60
    outline_thickness = 0
    dots_size = 0.25 # Scale of input-field height, 0.2 - 0.8
    dots_spacing = 0.55 # Scale of dots' absolute size, 0.0 - 1.0
    dots_center = true
    dots_rounding = -1
    outer_color = rgba(242, 243, 244, 0)
    inner_color = rgba(242, 243, 244, 0)
    font_color = rgba(202, 203, 214, 0.75)
    fade_on_empty = false
    placeholder_text = # Text rendered in the input box when it's empty.
    hide_input = false
    check_color = rgba(204, 136, 34, 0)
    fail_color = rgba(204, 34, 34, 0) # if authentication failed, changes outer_color and fail message color
    fail_text = $FAIL <b>($ATTEMPTS)</b> # can be set to empty
    fail_transition = 300 # transition time in ms between normal outer_color and fail_color
    capslock_color = -1
    numlock_color = -1
    bothlock_color = -1 # when both locks are active. -1 means don't change outer color (same for above)
    invert_numlock = false # change color if numlock is off
    swap_font_color = false # see below
    position = 0, -780
    halign = center
    valign = center
}

label {
  monitor = 
  text = cmd[update:1000] echo "$(~/.config/hypr/hyprlock/song-status.sh)"
  color = rgba(242, 243, 244, 0.75)
  font_size = 24
  font_family = SF Pro Text
  position = 20, 860
  halign = left
  valign = center
}

label {
  monitor =
  text = cmd[update:1000] echo "$(~/.config/hypr/hyprlock/network-status.sh)‎"
  color = rgba(242, 243, 244, 0.75)
  font_size = 26
  font_family = SF Pro Text
  position = -35, 860
  halign = right
  valign = center
}

label {
  monitor =
  text = cmd[update:1000] echo "$(~/.config/hypr/hyprlock/battery-status.sh)‎"
  color = rgba(242, 243, 244, 0.75)
  font_size = 29
  font_family = SF Pro Text
  position = -93, 860
  halign = right
  valign = center
}

label {
  monitor =
  text = cmd[update:1000] echo "$(~/.config/hypr/hyprlock/layout-status.sh)‎"
  color = rgba(242, 243, 244, 0.75)
  font_size = 25
  font_family = SF Pro Text
  position = -150, 860
  halign = right
  valign = center
}

label {
  monitor =
  text = cmd[update:1000] echo "$(date +"%A, %B %d")"
  color = rgba(242, 243, 244, 0.75)
  font_size = 30
  font_family = SF Pro Display Bold
  position = 0, 430
  halign = center
  valign = center
}

label {
  monitor = 
  text = cmd[update:1000] echo "$(date +"%k:%M")"
  color = rgba(242, 243, 244, 0.75)
  font_size = 153
  font_family = SF Pro Display Bold
  position = 0, 310
  halign = center
  valign = center
}


label {
    monitor =
    text = Fanny A.
    color = rgba(242, 243, 244, 0.75)
    font_size = 22
    font_family = SF Pro Display Bold
    position = 0, -675
    halign = center
    valign = center
}

label {
    monitor =
    text = Touch ID or Enter Password
    color = rgba(242, 243, 244, 0.75)
    font_size = 20
    font_family = SF Pro Display
    position = 0, -715
    halign = center
    valign = center
}

image {
    monitor =
    path = ~/.config/pfp.img 
    border_color = 0xffdddddd
    border_size = 0
    size = 160
    rounding = -1
    rotate = 0
    reload_time = -1
    reload_cmd = 
    position = 0, -555
    halign = center
    valign = center
}

